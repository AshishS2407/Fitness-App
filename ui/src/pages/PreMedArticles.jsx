import React from 'react'
import ArticleCards from '../components/ArticleCards'

const PreMedArticles = () => {
  return (

<ArticleCards isHome={true} showType="gain" userType={"premium"} />

)
}

export default PreMedArticles